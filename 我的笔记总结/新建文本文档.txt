一、使用gitHup

   如果在指定的文件夹地址栏输入cmd打开命令符提示窗口，输入git init（输入git的命令），报错不能执行，
那就是没有全局配置git命令，那就要打开C盘-->Program Files (x86)-->Git-->bin,找到bin文件夹打开，
然后复制地址栏上的地址，再打开电脑的高级系统设置-->点击环境变量-->在用户变量栏找到path，选中了点击
编辑，将刚才复制的路径添加到后面，注意用英文的分号隔开